[toolchain]
# Exact pin so devenv (rust-overlay) + CI share one toolchain and caches survive.
# rust-overlay reads this file; toolchain resolution is pure evaluation.
channel = "1.96.0"
components = ["rustfmt", "clippy"]
